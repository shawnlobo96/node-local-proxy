# Sample test with proxy

- Clone the repo
- Run `npm i`
- Add credentials in `index.js` desired capabilities.
- Define the proxy host and port for local as well as for webdriver.
- Save and run `npm test`
